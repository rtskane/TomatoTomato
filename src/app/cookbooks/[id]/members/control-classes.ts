// The one style for the small inline controls in the Share dialog — the role
// selects and the one-time link's label field — so the three components that
// draw them can't drift apart.
export const controlClass =
  "rounded-lg border border-border bg-background-control px-2 py-1 text-subheadline " +
  "outline-none focus:border-border-input-strong disabled:opacity-50";
